<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300">
    <Border Margin="10">
        <StackPanel>
            <!---Buttons-->

            <!---Select Storage-->
            <TextBlock Text="Select Storage" FontWeight="bold"/>
            <Grid>
                <ListView Margin="10" ItemsSource="{Binding Stores}"
                          SelectedItem="{Binding SelectedStore}"></ListView>
            </Grid>
            <TextBlock Text="Select Product" FontWeight="bold"/>
            <Grid>
                <ListView Margin="10" ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"></ListView>
            </Grid>


            <Button x:Name="CreateOrder"  Margin="0 10 0 0" VerticalAlignment="center" Grid.Column="0"  Content ="Create Order" Width="100" Height="Auto" />
        </StackPanel>
    </Border>
</Window>
